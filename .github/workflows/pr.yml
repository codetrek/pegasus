name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  test:
    name: Test and Coverage (95% Required)
    uses: ./.github/workflows/test.yml
    with:
      coverage-threshold: 95
